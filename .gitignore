# dsh-logcat — repo hygiene
node_modules/
*.log
.DS_Store
*.tgz

.npmrc.tmp

tmp-*

# build artifact: the dex itself is embedded as base64 in lib/injector-jar.js
# (rebuild with tools/injector/build.mjs)
lib/injector.jar
